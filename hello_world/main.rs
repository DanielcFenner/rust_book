// Main is the entry point to every rust program
// Rust indents with 4 spaces

fn main() {
    // ! means we are calling a macro, not a function
    println!("Hello, world!");
}

// To compile this in the terminal:
// rustc main.rs

// And to run it:
// ./main
